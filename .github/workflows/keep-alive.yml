name: Keep Supabase awake

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping production keep-alive
        run: curl -fsS --retry 3 --retry-delay 5 "https://datanerds-ai-training.vercel.app/api/keep-alive"
